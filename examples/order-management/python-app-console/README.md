# Python app console

This handwritten console is the smallest useful application layer. It creates a `UserContext`, asks the generated runtime to ensure SQLite schema, seeds through generated entities, then performs a governed query and audited save.

Run it from the parent example using the commands in [`../README.md`](../README.md).

